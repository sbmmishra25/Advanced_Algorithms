# 14 — Advanced Data Structures

Topics: binary heap, DSU, Fenwick tree, segment tree, sparse table, trie, balanced search trees, interval tree, B-tree concepts and Bloom filters.

DSU with path compression and union by rank/size has near-constant amortized cost O(alpha(n)).

Segment trees support common range queries and point/range updates in O(log n).