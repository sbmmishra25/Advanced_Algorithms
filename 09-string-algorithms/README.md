# 09 — String Algorithms

Topics: naive matching, KMP, Z algorithm, Rabin-Karp, trie, Aho-Corasick, suffix arrays, LCP arrays and rolling hash.

KMP preprocessing is O(m) and search is O(n).

Advanced: suffix automata, suffix trees and approximate string matching.